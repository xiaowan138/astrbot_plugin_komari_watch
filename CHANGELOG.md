# 更新记录

## [1.5.0] - 2026-09-18

### 修复

- **修复"所有节点离线"被误判为"WS 通道不可用"**：WebSocket 正常但没有在线节点时，插件此前会退回"历史时间戳新鲜度"判定，刚掉线的节点可能因记录尚新仍被判在线，拖延离线告警，还会对全部节点发起多余的历史请求。`_realtime` 现将"通道是否可用"与"在线列表是否为空"分开返回，仅在 WS 真正不可用时才走历史兜底。
- **修复历史趋势图可能折返**：历史记录渲染前先按时间戳排序，API 返回乱序时曲线不再来回折返。
- **`/komari_check` 不再吞掉失败**：检查失败时明确返回错误原因；成功时返回结果摘要（在线/离线数量、是否推送了新告警）。
- **过滤误配警告覆盖后台轮询**：`filter_mode=allow` 但 `filter_nodes` 为空的警告此前只在执行 `/komari_status` 时出现，现在后台检查也会提示。
- **节点查询支持多个关键词**：`/komari_status nodeA nodeB` 可同时匹配多个节点，此前只有第一个关键词生效。

### 新增

- **历史图表时间范围标注**：`/komari_history` 的每个节点卡片显示数据实际起止时间（如 `09-18 14:00 → 09-18 20:00`），不再只有"最近 N 小时"。
- **`/komari_top` 支持运行时长指标**：`/komari_top uptime` 按运行时长列出在线节点，便于快速找出刚重启或运行最久的机器。
- 测试新增：历史排序、时间范围标注、检查摘要、失败提示、uptime 榜单、多关键词选择等断言。

## [1.4.0] - 2026-09-05

### 修复

- **修复自 1.1.0 起全部 HTTP 请求在运行时失败的严重问题**：共享会话的获取方法与实例属性 `self._session` 同名，实例属性遮蔽方法后 `await self._session()` 直接抛 `TypeError`，又异常捕获吞掉转为"连接 Komari 失败"，导致状态查询、轮询与告警全部失效（语法检查无法发现）。方法更名为 `_get_session`，并新增会话创建锁，防止并发重复创建连接池。
- **修复关键词无匹配时的误导提示**：`/komari_status 节点名`、`/komari_realtime 节点名` 无匹配节点时，此前统一回复"Komari 没有返回节点"，现改为明确提示"没有匹配「xx」的节点"。
- **修复历史趋势图一律显示在线绿点**：历史卡片现按节点真实在线状态显示绿点/红点（该命令改用与状态查询一致的快照数据）。
- **修复离线节点显示"等待心跳"**：状态卡片对离线节点改显示"最后在线：X 分钟前"，无心跳记录时显示"状态：离线"。
- **高负载告警只列超标项**：告警详情从罗列 CPU/内存/磁盘全部数值改为只列超过阈值的项。
- **`/komari_mute all` 无绑定会话时**：明确提示"当前没有绑定任何会话"，不再回复静默成功。

### 新增

- **面板故障通知**：连续 `panel_fail_cycles`（默认 3）次检查失败后向绑定会话推送"面板不可达"告警，恢复时推送恢复通知，避免"监控自己挂了没人知道"。
- **节点重启检测**：在线节点运行时间明显回退（超过 30 秒）时推送"节点重启"通知，显示重启前后运行时长（`notify_restart` 可关闭）。
- **流量趋势图**：`/komari_history` 新增上下行流量双曲线（自动缩放），CPU/内存/磁盘与流量图均标注当前值与峰值。
- **长期离线每日提醒**：`long_offline_remind_hours` 大于 0 时，节点离线超过该时长后每日提醒一次，避免宕机被遗忘。
- **`/komari_top`**（别名 `/ktop`）：资源占用 Top 榜，如 `/komari_top mem 10`，支持 cpu/mem/disk 指标，仅统计在线节点。

### 测试

- 新增带 astrbot mock 的冒烟测试：覆盖告警全链路（离线/高负载/恢复/重启/长期离线/面板故障）、渲染输出（统计条、离线标签、XSS 转义、流量图、峰值标注）与命令层行为，共 32 项断言全部通过；并包含 `_get_session` 同名冲突的回归验证。

## [1.3.0] - 2026-09-04

### 新增

- **`/komari_help`**（别名 `/khelp`）：新增命令总览，一条消息看全全部命令用法。
- **`/komari_nodes`**（别名 `/knodes`）：列出全部节点名称及是否被过滤排除，便于填写 `filter_nodes` 与查询命令参数。
- **卡片头部统计**：状态卡片头部新增"共 N 节点 / 在线 X / 离线 Y"统计条，一眼掌握全局。
- **历史查询支持指定节点**：`/komari_history 6 nodeA` 可只看匹配节点的趋势曲线，参数顺序不限。
- **每天固定时刻日报**：新增 `status_report_time` 配置（本地时间 `HH:MM`，如 `09:00`），到点自动向绑定会话推送状态卡片；可与 `status_report_interval` 共存，先到先推，配置无效时打一次警告日志。
- **按会话静默**：`/komari_mute` 从全局静默改为默认只静默当前会话，加 `all` 参数可静默全部绑定会话；`/komari_unmute` 同理。旧版 `muted_until` 全局字段启动时自动迁移，过期静默条目定期清理。

### 修复

- **修复 `/komari_realtime` 把掉线节点显示为在线**：此前合并静态节点后一刀切标记"在线"，与该命令"真正实时通道"的定位相悖；现在只有出现在 WebSocket 返回里的节点才显示在线。
- **修复混合模式下 WS 在线节点被误判离线**：此前只要任一节点缺内存/磁盘指标，就会为全部节点拉历史记录，导致 WS 在线状态还要过历史时间戳新鲜度校验（窗口仅 3×轮询间隔）；记录稀疏时（如轮询 15 秒、记录间隔分钟级）WS 刚上报在线的节点也会被判离线，凑满宽限周期后触发假离线告警。现在 WS 通道可用时其在线列表为权威信号，新鲜度校验仅用于 WS 不可用的纯历史兜底场景。
- **修复数字时间戳解析**：`_parse_time` 现在兼容 unix 秒/毫秒时间戳（此前仅支持 ISO 字符串；若服务端记录时间为数字，历史兜底的在线判定与相对时间显示会全部失效）。
- **修复最新记录选择**：历史兜底取"最新"记录改用数值时间比较，此前按字符串比较可能在位数不同的时间戳上选错记录。
- **减少不必要请求**：仅对缺失内存/磁盘指标的在线节点拉取历史记录，不再因个别节点缺指标而对全部节点发起 N 次请求。
- **WS 读取容忍非数据首帧**：WebSocket 首条消息若是 ack/pong 等非数据帧，自动继续读取下一条（最多 3 条）再解析，解析逻辑拆分为独立函数。
- **其他**：`/komari_realtime` 不再在持有检查锁期间 yield；移除未使用的 `import re`。

## [1.2.0] - 2026-08-27

### 新增

- **历史资源趋势图**：新增 `/komari_history`（别名 `/khistory`、`/历史`），复用 `html_render` 渲染 CPU / 内存 / 磁盘的 SVG 曲线卡片，支持 `1-24` 小时范围。
- **告警合并**：同一检查周期内多个节点离线或恢复会合并成一条汇总告警，避免在节点数量较多时刷屏。
- **临时静默**：新增 `/komari_mute <分钟>`（默认 30 分钟）与 `/komari_unmute`，静默期内不推送任何告警或定时状态卡。
- **告警历史查询**：新增 `/komari_alerts`（别名 `/kalerts`），在 `state.json` 中维护最近 50 条告警的可查历史。
- **相对更新时间**：状态卡片与报告中的"更新时间"改为相对时间（如"3 分钟前"）。

### 修复

- **修复高负载"恢复"误报**：负载恢复通知现在仅在节点仍在线时触发，避免节点掉线时同时收到"离线告警"与"负载恢复"的矛盾消息。
- **修复过滤配置静默失效**：当 `filter_mode` 为 `allow` 但 `filter_nodes` 为空时，之前会静默忽略所有节点；现在会打出一行醒目日志提示配置问题。
- **修复故障时无退避**：Komari 整体不可达时连续失败会按指数退避（封顶 30 分钟），成功即重置，降低无效重试与日志噪音；手动 `/komari_check` 不参与退避统计。
- **并发保护与健壮性**：`/komari_status`、`/komari_realtime` 补上检查锁，避免与后台轮询并发重复请求；WebSocket 地址构造正确处理 `https/wss` 大小写；清理未使用的变量。

## [1.1.0] - 2026-08-23

### 新增

- **节点过滤**：新增 `filter_mode`（`none`/`allow`/`deny`）与 `filter_nodes` 配置，支持白名单或黑名单节点监控（按名称/主机名/id/uuid 子串匹配）。
- **指定节点查询**：`/komari_status`、`/komari_realtime` 支持携带节点名只看指定节点。
- **恢复通知带时长**：离线与高负载恢复消息附带持续时长。
- **定时状态推送**：新增 `status_report_interval`（小时）配置，后台监控按间隔自动向绑定会话推送状态卡片。
- **`/komari_realtime` 定位调整**：改为真正的实时查询（不经历史兜底），WebSocket 不可用时明确提示改用状态命令。

### 修复

- **修复离线节点误报高负载**：节点离线时跳过其高负载判定，避免用陈旧指标对死节点告警。
- **修复连接滥用**：`_get_json`、WebSocket、历史记录改用复用的 `ClientSession`，`terminate` 时统一关闭。
- **修复 N+1 串行请求**：历史记录兜底改为 `asyncio.gather` 并发拉取各节点。
- **WS 读取健壮性**：不再写死读取 3 条消息，支持 BINARY 帧、忽略 ping/pong、异常处理补充 `TypeError`。
- **清理 `state.json` 无限增长**：新增 `prune_missing_cycles` 配置，节点从服务器消失后自动清理其监控状态。

## [1.0.1] - 2026-08-12

### 修复

- **修复告警被冷却期永久吞掉的问题**：离线/高负载告警判断由 `==` 改为 `>=`。此前若节点在冷却期内再次触发阈值，计数器会越过触发点，`==` 判断永不再成立，导致该次宕机全程无告警。
- **修复 WebSocket 不可用时离线检测失效**：历史记录兜底模式下，节点即使心跳早已停止，只要 1 小时内存在残留记录就会被判定为"在线"，离线告警永不触发。现在兜底数据会校验记录时间的新鲜度。
- **修复并发竞态**：后台轮询与 `/komari_check` 可能同时执行检查并互踩状态，导致重复告警。现在检查过程由 `asyncio.Lock` 串行化。
- **修正插件注册信息中的仓库地址**：`xiaowan/astrbot_plugin_komari_watch` → `xiaowan138/astrbot_plugin_komari_watch`。

### 其他

- 更新 README 中的开源说明与仓库地址占位。
